module.exports = {
    // Do not include a trailing slash!
    siteUrl: `https://blog.danielkawalsky.com`,

    // Enable infinite scroll (default: true)
    infiniteScroll: true,

    // Initial number fetched, scrolling lazy loads posts one by one
    // If infinite scroll is disabled: maximum number of post shown per page
    postsPerPage: 3,

    // This allows an alternative site title for meta data for pages.
    siteTitleMeta: `Dan Codes It`,

    // This allows an site description for meta data for pages.
    siteDescriptionMeta: `Some of my programming finds, curiosities, and projects`,

    // Used for App and Offline manifest e.g. Mobile Home Screen
    shortTitle: `Code Blog`,
    siteIcon: `favicon.png`,
    backgroundColor: `#e9e9e9`,
    themeColor: `#728950`,

    // Show more logs for debugging purposes (default: false)
    verbose: false,

    // Severity for verbose mode: (`info`, `warn`, `error`)
    severity: `info`,

}
